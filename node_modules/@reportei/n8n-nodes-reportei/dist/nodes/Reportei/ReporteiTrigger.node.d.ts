import { IHookFunctions, IWebhookFunctions, INodeType, INodeTypeDescription, ILoadOptionsFunctions, INodePropertyOptions, IWebhookResponseData } from 'n8n-workflow';
export declare class ReporteiTrigger implements INodeType {
    description: INodeTypeDescription;
    webhookMethods: {
        default: {
            checkExists(this: IHookFunctions): Promise<boolean>;
            create(this: IHookFunctions): Promise<boolean>;
            delete(this: IHookFunctions): Promise<boolean>;
        };
    };
    methods: {
        loadOptions: {
            getClients(this: ILoadOptionsFunctions): Promise<INodePropertyOptions[]>;
        };
    };
    webhook(this: IWebhookFunctions): Promise<IWebhookResponseData>;
}
